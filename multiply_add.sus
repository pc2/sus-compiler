
/* a module to test the syntax */
module MultiplyAdd : i32 a, i32 b, i32 c -> i32 result {
	//module beelqzd 
	// temporary variable
	//i32 tmp = a * b;
	result = tmp + cooo * 30 + 5;

	/*{
		//[
		beep boop;
	}

	int values[5];

	int (&values) [5]

	int[5]& values;*/
}
