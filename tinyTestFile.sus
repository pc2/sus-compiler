// USE THIS FILE TO TEST SMALL SAMPLES OF SUS CODE IN ISOLATION. 
// DO NOT COMMIT
// To use with VSCode debugger, name the module "TEST", and in the code use `if crate::debug::is_enabled("TEST") {}` to enable breakpoint code

module TEST {

}

/// Test the workaround for 1-bit Conditional Assigns become don't care
module test_vivado_bug__ {
	for int SZ in 1..4 {
		bool[SZ] x = Repeat(false)
		bool[SZ] y = x

		int#(FROM: 0, TO: SZ) u = 0
		int v = u
	}
}
